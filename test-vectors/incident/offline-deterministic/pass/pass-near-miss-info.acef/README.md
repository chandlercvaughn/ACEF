# near_miss informational marker (ACEF-087 INFO)

A public incident_card whose `harm_core.realization` is `near_miss`. ACEF-087 is an INFORMATIONAL marker (§5.5) — surfaced at INFO severity, NEVER a failure. The bundle is a PASS that carries an info diagnostic.

**Expected code(s):** none (pass)
