# severity_vector parse failure (ACEF-082)

A public incident_card whose `severity_vector` does not parse against the ACEF-SEV:1.0 grammar (a truncated vector). The validator raises ACEF-082.

**Expected code(s):** ACEF-082
